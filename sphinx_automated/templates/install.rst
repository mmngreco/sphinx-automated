==========
Installing
==========

User mode : static version of the code.

.. code-block:: bash

   pip install git+{URL}

Developer mode : dynamic version of the code.

.. code-block:: bash

   git clone {URL}
   cd {PRJ_NAME}
   pip install -e .

